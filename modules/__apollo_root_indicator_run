# vim:ft=zsh
################################################################
# Segment to print an icon if user is root.

__apollo_root_indicator_run() {
  __APOLLO_RETURN_MESSAGE="${${(%):-%#}:#%}"
}

__apollo_root_indicator_run "$@"
