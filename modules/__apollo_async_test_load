# vim:ft=zsh

__apollo_async_test_run() {

  if [[ -n "${__APOLLO_ASYNC_RESULTS[async_test]}" ]]; then
    __APOLLO_RETURN_MESSAGE="${__APOLLO_ASYNC_RESULTS[async_test]}"
  else
    __APOLLO_RETURN_MESSAGE="No async"
  fi

}

__apollo_async_test_async() {

  echo "${(%):-%*}"

}
