# vim:ft=zsh
################################################################
# Command number (in local history)

typeset -g APOLLO_HISTORY_FG="white"
typeset -g APOLLO_HISTORY_BG="grey50"

_apollo_history_run() {
  _APOLLO_RETURN_MESSAGE="%h"
}
